# 项目：个性化离焦剂量探索系统（贝叶斯 Sigmoid + 主动学习）

## 一、项目概述

你需要开发一个 **Python 工具**，基于贝叶斯 Sigmoid（四参数逻辑斯蒂）模型和主动学习策略，为每个受试者动态推荐下一个离焦测量剂量。系统以最少实验次数（通常 2\~5 次）估计个体离焦阈值（ED50）和最佳剂量。

**核心特点**：

- 双点启动（2.0D, 4.0D）避免单点方向误判
- 贝叶斯 MCMC 推断（PyMC）量化不确定性
- 主动学习采集函数：后验方差（探索） + 期望改进（开发）
- 自动停止条件
- 无需 Jupyter

**交付物**：一个完整的 Python 包或单个脚本，包含模拟测试数据的功能，可一键运行演示整个流程。

***

## 二、模型与算法详细规格

### 2.1 剂量-反应模型（Sigmoid）

\[
y = \text{baseline} + \frac{\text{max\_response} - \text{baseline}}{1 + \exp\left(-\text{slope} \cdot (x - \text{threshold})\right)} + \varepsilon,\quad \varepsilon \sim \mathcal{N}(0, \sigma^2)
]

参数先验（基于文献和预实验）：

| 参数            | 先验分布                  | 约束             |
| :------------ | :-------------------- | :------------- |
| baseline      | Normal(0, 5)          | 截断 \[0, 10]    |
| max\_response | Normal(20, 10)        | 截断 (0, 50]     |
| slope         | LogNormal(ln(3), 0.5) | 无              |
| threshold     | Normal(2.5, 1.0)      | 截断 \[0.5, 6.0] |
| sigma         | HalfNormal(5)         | 无              |

### 2.2 贝叶斯推断

- 库：**PyMC** (≥5.10)
- MCMC 设置：
  - 链数：4
  - 调优步数：2000
  - 后验样本数：1000
  - 目标接受率：0.85
  - 收敛诊断：(\hat{R} < 1.05)，ESS > 200

### 2.3 主动学习策略

支持从 CSV 文件批量导入受试者的历史测量数据（跳过固定双点启动，直接运行主动学习）。

**双点启动**（无csv文件数据时候的默认方法，后续会删除）：

- 第一轮推荐：`2.0 D`
- 第二轮推荐：`4.0 D`

**采集函数**（从第三轮开始）：

- **后验方差**（第3轮）：`acq(x) = Var[y(x) | data]`
- **期望改进**（第4轮起）：`acq_EI(x) = E[max(0, y(x) - y_best)]`，其中 `y_best` 是当前观测到的最大反应。

搜索空间：`0.5 D` 到 `6.0 D`，离散网格 100 点。

### 2.4 停止条件

满足任一即停止：

1. 最佳剂量处的后验标准差 < `2.0 μm`
2. 已测次数 ≥ `5`
3. 最高剂量 ≥ `6.0 D` 且最大反应 < `10 μm`（低反应者）

***

## 三、软件架构与模块

项目结构（单文件或包均可，推荐包结构）：参考即可，不必按照这个开发

```
defocus_bayesian/
├── __init__.py
├── model.py          # SigmoidModel 类（PyMC 建模、拟合、预测）
├── acquisition.py    # 采集函数
├── learner.py        # SigmoidActiveLearner 主类
├── simulate.py       # 模拟数据生成器（用于测试）
├── demo.py           # 演示脚本：模拟一个受试者，打印流程
└── test.py           # 单元测试（可选）
```

<br />

***

## 四、测试数据与验证要求

你需要**在代码中包含模拟数据生成器**，以便演示和测试。要求：

1. **生成 100 个虚拟受试者**，每个受试者的真实参数从先验分布中随机采样。
2. **运行主动学习流程**，记录每个受试者的测量次数、最终阈值估计误差（与真实阈值的绝对差）。
3. **输出统计报告**：
   - 平均测量次数
   - 阈值估计的均方根误差（RMSE）
   - 成功收敛的比例（停止条件因不确定性达标而触发的比例）
4. **可选**：对比双点启动 vs 单点启动（2.5D 启动）的效果，打印对比结果。

示例输出：

```
模拟 100 个受试者...
平均测量次数: 3.45
阈值 RMSE: 0.32 D
收敛率 (因不确定性停止): 87%
```

**测试脚本**（`test_simulation.py`）应当可以独立运行，不依赖 Jupyter。

***

## 五、环境与运行说明

### 5.1 依赖，写成requirement模式，我都用最新的包，避免冲突

```
pymc>=5.10
numpy>=1.24
scipy>=1.10
matplotlib>=3.5   # 可选，用于可视化
arviz>=0.17       # 可选，用于后验诊断
```

### 5.2 运行方式

<br />

### 5.3 可视化

提供一个 `plot.py`，可绘制后验剂量-反应曲线和采集函数，等全部相关研究所需曲线

***

## 六、代码风格与质量要求

- 使用类型注解（`typing`）
- 每个函数/类有 docstring
- 使用 `logging` 模块记录关键步骤（可选）
- 单元测试至少覆盖模型拟合和采集函数的基本行为

***

## 七、交付清单

1. **所有 Python 源文件**（按上述结构）
2. **requirements.txt**
3. **README.md**：包含安装、运行示例、参数说明
4. **test\_simulation.py**：批量模拟测试脚本
5. **demo.py**：功能演示脚本

**注意**：不需要 Jupyter Notebook 文件，全部为 `.py` 脚本。

***

## 八、示例运行流程（期望输出）

```
$ python demo.py
真实阈值: 2.87 D
测量剂量 2.00 D → 反应 3.2 μm
测量剂量 4.00 D → 反应 14.5 μm
训练 GP... (实际是贝叶斯 Sigmoid)
推荐下一剂量: 3.30 D (后验方差)
测量剂量 3.30 D → 反应 18.1 μm
推荐下一剂量: 2.90 D (期望改进)
测量剂量 2.90 D → 反应 17.8 μm
最佳剂量处后验标准差=1.8 μm < 2.0，停止。
估计阈值: 2.85 ± 0.25 D
真实阈值: 2.87 D
```

***

## 九、扩展要求

- <br />
- 支持保存和加载模型后验（`arviz.to_netcdf`）。
- 提供命令行接口（argparse）：
  ```bash
  python run.py --subject data/subject1.json --output results/
  ```

***

**请按照以上规范生成完整的 Python 代码。代码应当可以直接本地运行，无需额外修改（除了依赖安装）。**
